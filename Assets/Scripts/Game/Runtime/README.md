# Runtime

Unity scene and application lifecycle code lives here.

Use this layer for `MonoBehaviour` bridges, dependency wiring, tick orchestration, save/load entry points, and managers that connect Core simulation logic with Unity scenes and assets.
