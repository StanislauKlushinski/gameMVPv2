# UI

UI Toolkit controllers and presentation code live here.

Use this layer for view controllers, binding adapters, display formatting, and UI events. Keep business rules in Core or Runtime services so UI code remains a thin presentation layer.
