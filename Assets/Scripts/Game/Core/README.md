# Core

Pure C# gameplay logic lives here.

Use this layer for value types, serializable state, deterministic economy services, and shared utilities. Keep it free of `UnityEngine` dependencies so the code can be unit-tested without scene or asset setup.
