# Data

Unity-authored configuration definitions live here.

Use this layer for `ScriptableObject` types such as resources, recipes, buildings, technologies, epochs, cross-epoch links, and global game configuration. Data definitions may reference Core types, but should not own runtime simulation state.
